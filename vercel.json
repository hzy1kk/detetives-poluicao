{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd detetives-poluicao && npm install && npm run build",
  "outputDirectory": "detetives-poluicao/dist",
  "installCommand": "cd detetives-poluicao && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
